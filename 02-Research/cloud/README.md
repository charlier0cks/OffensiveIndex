# Cloud

AWS/Azure/GCP attack paths, IAM abuse, metadata services, misconfigurations.

## Notes in this domain

```dataview
LIST
FROM "OffensiveIndex/02-Research/cloud"
WHERE file.name != "README"
```