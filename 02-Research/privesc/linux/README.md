# Linux Privilege Escalation

SUID/SGID, sudo abuse, cron, capabilities, kernel exploits, PATH hijacking, etc.

```dataview
LIST
FROM "OffensiveIndex/02-Research/privesc/linux"
WHERE file.name != "README"
```